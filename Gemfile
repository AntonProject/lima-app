source "https://rubygems.org"

# Fastlane drives the store uploads from CI (see .github/workflows/release.yml).
# Pin a recent 2.x; CI runs `bundle install` so the version is reproducible.
gem "fastlane", "~> 2.222"
